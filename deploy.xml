<deploy>
<files>
    <path>~/FileCabinet/SuiteScripts/BlueCollar/*</path>
  </files>
  <objects>
    <path>~/Objects/lists/*</path>
    <!-- Records excluded from deploy — fields manually configured as List/Record
         in NS UI to reference BC SuiteApp records (project, cost code, change order).
         SDF would overwrite those back to INTEGER. Only re-add if creating fresh. -->
    <!-- <path>~/Objects/records/*</path> -->
    <path>~/Objects/fields/*</path>
    <path>~/Objects/scripts/*</path>
  </objects>
</deploy>
