{
  "name": "bc-cashflow",
  "version": "1.0.0",
  "description": "BlueCollar Cash Flow Forecasting — SDF Account Customization Project",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "validate": "npx suitecloud project:validate",
    "deploy:dryrun": "npx suitecloud project:deploy --dryrun",
    "deploy": "npx suitecloud project:deploy"
  },
  "devDependencies": {
    "@oracle/suitecloud-unit-testing": "^1.3.0",
    "jest": "^29.7.0"
  }
}
